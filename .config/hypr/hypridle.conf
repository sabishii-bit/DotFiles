general {
    after_sleep_cmd  = hyprctl dispatch dpms on
    ignore_dbus_inhibit = false
}

listener {
    timeout  = 600                 # 10 minutes of idle
    on-timeout = systemctl suspend # suspend to RAM (sleep)
}
